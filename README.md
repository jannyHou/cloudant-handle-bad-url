### A sample app test cloudant URL

Contains two test cases:

  - Call `Cloudant()` with a well formatted URL, error is caught.
  - Call `Cloudant()` with a bad URL, error is not caught and app crashes.
